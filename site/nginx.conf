server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    server_tokens off;

    # Security headers. These must equal Traefik's `secure-headers` middleware
    # (kubelab `infra/k8s/base/edge/secure-headers.yaml`), because through the
    # ingress the edge's values REPLACE these and the divergence is invisible
    # from outside — measured as `SAMEORIGIN` here against `DENY` on the wire
    # (WEB-111). What the origin declares is what serves when the middleware is
    # absent: `docker run`, a port-forward, or a route that omits it.
    #
    # HSTS, CSP and the TLS redirect are deliberately not here. This server
    # listens on plain 8080 and TLS terminates at the edge, so they are the
    # edge's to send. `tests/nginx-security-headers.test.mjs` asserts the rest.
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()" always;

    # Static assets: long cache (inherit security headers via include)
    location ~* \.(css|js|woff2?|png|webp|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()" always;
        try_files $uri =404;
    }

    # The release marker (WEB-106): never answered from a cache. A stale value
    # here is a confidently wrong answer to "which release is prod running",
    # which is worse than the 404 it replaces — prod served a June build for
    # eleven releases and every available signal looked fine (#190).
    #
    # `add_header` in a location REPLACES the server-level set rather than
    # adding to it, so the security headers are repeated here for the same
    # reason the static-assets block above repeats them.
    location = /version.json {
        add_header Cache-Control "no-cache" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()" always;
        try_files $uri =404;
    }

    # Astro static routes
    location / {
        try_files $uri $uri/index.html =404;
    }

    # Custom 404
    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
