{
  "$comment": [
    "Reviewer pool for /adversarial-review (HARNESS-071, #955).",
    "",
    "Ordered allow-list of models permitted to SIGN a review.md. The first entry",
    "is the launcher's primary; the whole array is the gate's allow-list, which",
    "dotf spec archive enforces via checkReviewGate.",
    "",
    "Standing rule: an adversarial review never runs on an Anthropic model. The",
    "reviewer must not be the implementer, and Claude implements nearly every",
    "change in this repo — BUG-074 was reviewed twice by claude-opus-5 before",
    "anyone noticed, which is the incident this file exists to prevent.",
    "",
    "Entries are model IDS, not tool names, and that distinction is load-bearing:",
    "`agy` also serves claude-opus-4-6-thinking and claude-sonnet-4-6, so pinning",
    "the tool would guarantee nothing. Pinning the id does.",
    "",
    "`id` is the canonical string a reviewer records in review.md and the gate",
    "matches on. `provider`/`model` are the launcher's flags, kept explicit rather",
    "than parsed out of `id`: BUG-074 round 3 ran on the right model only because",
    "~/.pi/agent/settings.json on one machine happened to default to it — pi's own",
    "default provider is google, and that file is unversioned per-machine state.",
    "A pin that depends on a default is not a pin.",
    "",
    "Reasoning-class only. The latency-optimised daily models (nan/qwen3.6,",
    "nan/gemma4, the gemini flash tier) are deliberately absent: a reviewer that",
    "PASSes cheaply is worse than no gate, because it converts the gate into a",
    "green checkmark. This file is read once per spec, so slow is fine.",
    "",
    "This is the load-bearing slice of H-044, not a replacement for it. H-044's",
    "model-map.json maps a neutral tier (top|mid|low) to a provider per agent — a",
    "different shape. Kept in a separate file so H-044 need not migrate this one."
  ],
  "pool": [
    {
      "id": "nan/deepseek-v4-flash",
      "runner": "pi",
      "provider": "nan",
      "model": "deepseek-v4-flash",
      "role": "primary",
      "why": "1M context and a mandatory reasoning chain — roughly 4x slower than qwen3.6 on trivial prompts, which is the correct trade for a gate that runs once per spec. Exercised: BUG-074 round 3 re-ran the mutation battery itself rather than trusting the spec's table. FLAGGED 2026-08-21 (#1205), not changed: this same model returns a 200 with an EMPTY body under PR-Agent — it spends its OUTPUT budget on the reasoning chain and never writes the answer, measured at 4m15s of silence on kubelab#1202 run 32440498259. That is the behaviour already described in the mimo entry below, from this pool's own harness. PR-Agent's chain was inverted to lead with mimo; this pool's ordering was NOT, because its runner and prompt differ and no measurement of THIS harness exists yet. Read that as an open question about the primary, not as an endorsement of it."
    },
    {
      "id": "agy/gemini-3.1-pro-high",
      "runner": "agy",
      "model": "gemini-3.1-pro-high",
      "role": "fallback",
      "why": "A second provider family, independent of both NaN and Anthropic — provider diversity rather than merely a different model. Pro tier at high effort; the flash tiers are excluded for the same reason qwen3.6 is. agy has no --provider flag; the model id selects the family."
    },
    {
      "id": "nan/mimo-v2.5",
      "runner": "pi",
      "model": "mimo-v2.5",
      "role": "fallback",
      "why": "Admitted 2026-08-21 as the SECOND fallback, after the provider-diverse one, and the ordering is meaning rather than sequence: gemini answers a NaN OUTAGE, mimo only answers NaN SATURATION. The weaker property must not be promoted above the stronger one. What the evaluation established: given the same planted-defect snippet, mimo returned a real, correct finding, and it is reasoning-class by profile (1M context, reasoning chain) against qwen3.6 and gemma4's 262K, which is the line this pool draws. What it did NOT establish: a quality ranking against deepseek — deepseek never produced content under the same harness, spending 29,299 characters of reasoning and returning finish_reason=length with an empty body. That is an instrumentation result, not a capability one, and it is stated here so nobody reads this entry as 'mimo matched deepseek'. Carried with its verdict from mlorentedev/dotfiles#1116 rather than re-derived. Its role in .pr_agent.toml is now PRIMARY, not fallback, and this sentence said otherwise until #1205 — the inversion landed there and the description did not follow it, which is the two-files-disagreeing failure the config's own comment warns about, in miniature. The reason it leads: deepseek's characteristic failure is a 200 with an empty body, which raises no exception, so with it in front LiteLLM's chain could never fire. mimo's failure mode is saturation, a 429, which does raise and does fall through. This pool keeps its OWN ordering unchanged — different runner, different prompt, no measurement of this harness yet.",
      "provider": "nan"
    }
  ]
}
