---
import UnsubscribeForm from '../components/forms/subscribe/UnsubscribeForm.astro';
import IndexLayout from '../layouts/IndexLayout.astro';

const { lang } = Astro.props;

const title = 'Cancela tu suscripción';
const subtitle = 'Que tengas un gran día';
---

<IndexLayout title={title} description={subtitle} lang={lang}>
  <UnsubscribeForm />
</IndexLayout>
