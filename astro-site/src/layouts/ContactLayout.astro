---
import SectionHeader from '../components/sections/SectionHeader.astro';
import BaseLayout from './BaseLayout.astro';

const { title, description, contactTitle, contactSubtitle } = Astro.props;
const pubDate = new Date();
const tags = ['defaultTag'];
const buttonText = 'defaultButtonText';
const resourceId = 'defaultResourceId';
const fileId = 'defaultFileId';
---

<BaseLayout
  title={title}
  description={description}
  pubDate={pubDate}
  resourceId={resourceId}
  fileId={fileId}
  tags={tags}
  buttonText={buttonText}
>
  <SectionHeader title={contactTitle} subtitle={contactSubtitle} />
  <article class="text-gray-800 max-w-3xl mx-auto p-4">
    <div class="prose lg:prose-xl mx-auto">
      <slot />
    </div>
  </article>
</BaseLayout>
